from threading import current_thread


def safe_get_thread_attribute(attribute_name: str):
    try:
        return current_thread().__getattribute__(attribute_name)
    except AttributeError:
        return None
