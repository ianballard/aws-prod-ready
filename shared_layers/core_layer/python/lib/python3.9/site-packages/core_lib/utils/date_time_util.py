import time
from datetime import datetime


def get_current_utc_time_millis():
    return int(round(time.time() * 1000))


def get_current_utc_datetime_iso():
    return datetime.utcnow().isoformat()
